spring:
  application:
    name: service-offering

  datasource:
    url: jdbc:postgresql://localhost:5432/services-db
    username: postgres
    password: Saumya@70
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8085

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8070/eureka/